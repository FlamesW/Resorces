-- // This is for the script, It is not meant to be executed.
local CreditsSection1 = getgenv().CreditsTab:Section({Side = "Left"});

CreditsSection1:Header({Text = "Credits Wall"});

CreditsSection1:Divider();

CreditsSection1:Spacer();

CreditsSection1:Paragraph({Header = "Script Creator",Body = "Mommy Flames <3"});

CreditsSection1:Spacer();

CreditsSection1:Paragraph({Header = "Some Sources",Body = "Sirius Team <3"});

CreditsSection1:Divider();

CreditsSection1:SubLabel({Text = "Thanks to all these amazing people <3"});

local CreditsSection2 = getgenv().CreditsTab:Section({Side = "Left"});
CreditsSection2:Header({Text = "Contact me"});

CreditsSection2:Divider();

CreditsSection2:Button({Name = "Get in touch here.",Callback = function()
	Window:Notify({Title = Window.Settings.Title,Description = "Discord link copied to clipboard!",Lifetime = 3});
	setclipboard("https://discord.com/users/656455297979908106");
end})

CreditsSection2:SubLabel({Text = "Please dont contact me for no reason~"});

local CreditsSection3 = getgenv().CreditsTab:Section({Side = "Left"});

CreditsSection3:Button({Name = "Join Discord Server",Callback = function()
	Window:Notify({Title = Window.Settings.Title,Description = "Discord link copied to clipboard!",Lifetime = 3});
	loadstring(game:HttpGet("https://raw.githubusercontent.com/FlamesW/Main/home/Discord"))();
end})

CreditsSection3:SubLabel({Text = "https://dsc.gg/Flames-Community"});

local CreditsSection4 = getgenv().CreditsTab:Section({Side = "Right"});

CreditsSection4:Header({Text = "© 2024 Flames. All rights reserved."});

CreditsSection4:Divider();

CreditsSection4:Spacer();

CreditsSection4:Paragraph({Header = "License Grant",Body = "Permission is granted to use this script for personal or educational purposes only."});

CreditsSection4:Spacer();

CreditsSection4:Spacer();

CreditsSection4:Paragraph({Header = "Restrictions",Body = ""});

CreditsSection4:SubLabel({Text = "- You may not copy, distribute, or modify this script."});

CreditsSection4:SubLabel({Text = "- It is allowed to be used for commercial purposes."});

CreditsSection4:SubLabel({Text = "- Any other use of this script requires explicit permission from the copyright holder."});

CreditsSection4:Divider();

CreditsSection4:Label({Text = "Disclaimer"});

CreditsSection4:SubLabel({Text = 'This software is provided "as-is", without any express or implied warranty. The author is not responsible for any damages resulting from the use of this script.'});

-- // Temporary!!!
task.spawn(function()
if not getgenv().Settings["PerformanceMode"] then
    getgenv().Window:Dialog({Title = Window.Settings.Title,Description = "You are missing some settings please confirm so the new update gets copied to clipboard.",Buttons = {{Name = "Confirm",
    Callback = function()
        task.spawn(function()
            setclipboard([[
                getgenv().Settings = { -- // true = on | false = off
                    ["Interface"] = {
                        ["VisibilityUI"] = "V", -- // Key you want to be pressed to Show/Hide UI.
                        ["CustomThemeUI"] = {0,0,0}, -- // Soft colors are recommended...
                        ["DraggingStyle"] = 2, -- // (1 or 2) choose what kind of dragging style you want.
                        ["AcrylicBlur"] = true, -- // Optional, but recommended with a blur since UI is big
                    },
                    ["General"] = {
                        ["MultipleConfig"] = true, -- // When turned on, each Roblox account will have its unique config.
                        ["StreamingMode"] = false, -- // Optional for showcasers and streamers.
                        ["UpdatePrompts"] = true, -- // Will notify you when there's a new update.
                    },
                    ["Security"] = {
                        ["ModeratorDetections"] = true, -- // Recommended to enable this.
                        ["SnitchDetections"] = false, -- // Optional, but recommended to use it.
                    },
                    ["PerformanceMode"] = true, -- // Recommended for low-end devices.
                };
            ]])            
        end)
    end,},{Name = "Cancel"}}})
end
end
